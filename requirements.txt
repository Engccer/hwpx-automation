# 편집 명령(--find/--set-cell/--split-cell 등)
python-hwpx>=2.8.3
# lxml: 6.x 호환 검증 완료(hwpx_edit.py가 lxml.etree를 직접 사용). 상한 제거.
# python-hwpx가 metadata에 lxml<6를 강제하지만 실측상 lxml 6에서 정상 동작하므로
# pip의 의존성 충돌 경고는 무시 가능(check-stack-updates known_overrides restore_after 참조).
lxml>=5.4

# 변환(--to-md): 엔진을 단일 소스로 관리하는 독립 패키지
# PyPI·GitHub(Engccer/hwpx-tomd) 공개. 엔진 자체를 수정할 때만 로컬 editable: pip install -e path/to/hwpx-tomd
# 버전 핀: 0.x 동안 minor 올림을 자동 수용(<1.0.0). 엔진(hwpx-tomd)·소비자를 같은 작성자가
# 관리하므로 breaking 변경 시 함께 대응한다. 1.0.0(stable) 도달 시 >=1.0.0,<2.0.0 등으로 다시 조인다.
hwpx-tomd>=0.2.0,<1.0.0

# 선택: COM 네이티브 파이프라인(hwpx_com.py): Windows + 한컴오피스 전용.
# pyhwpx가 pywin32·pandas·openpyxl 등을 함께 끌어오므로 기본 설치에 포함하지 않는다.
# 필요 시: pip install pyhwpx   (1.7.2에서 검증, 2026-06-10)
